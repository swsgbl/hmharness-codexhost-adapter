# Verification Record / 验证记录

## English

The following checks were run on 2026-09-10:

```powershell
# CodexHost source tree
npm run typecheck
npx --no-install vitest run --config tests/vitest.config.js `
  packages/protocol-core/test/codex-ui-projector.test.ts `
  packages/adapters/hmharness/test/hmharness-adapter.test.ts `
  packages/host-runtime/test/app-server-host.test.ts

# HM bridge
node --test packages/codexhost-bridge/test/bridge.test.mjs

# Patch applicability in a detached worktree at upstream base commit
git worktree add --detach <temporary-path> 0b76b9de8c6f551506287d9a12905f9fdc860da2
git -C <temporary-path> apply --check `
  G:\hmharness-codexhost-adapter\patches\codex-host\0001-add-hmharness-adapter-and-streaming-bridge.patch
```

Results:

- Typecheck: passed.
- Targeted CodexHost tests: 3 files and 170 tests passed.
- Bridge tests: 2 tests passed.
- Patch apply check: passed.
- Live smoke: HMHarness thread and turn reached `completed`; result availability was `available`; no HM bridge `run` process remained; renderer text contained both `HMH-DONE` and the final result with zero occurrences of `正在思考`.

## 中文

2026-09-10 已执行以下验证：

- CodexHost `npm run typecheck` 通过。
- CodexHost 定向回归 3 个文件、170 个用例通过。
- HM bridge Node 测试 2 个用例通过。
- 精选补丁在官方基准提交 `0b76b9d` 的临时 detached worktree 中 `git apply --check` 通过。
- 真实桌面端冒烟：HMHarness thread 和 turn 均为 `completed`，result 可用，bridge `run` 进程退出，Renderer 中 `正在思考` 出现次数为 0。

