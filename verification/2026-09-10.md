# Verification Record / 验证记录

## English

CodexHost PR branch `swsgbl:hmharness-adapter`, base `25fb54f`, head `3de8496`:

```powershell
npm install --ignore-scripts
npm run build:typescript
npm run typecheck
npm run lint
npx vitest run --config tests/vitest.config.js `
  packages/protocol-core/test/codex-ui-projector.test.ts `
  packages/adapters/hmharness/test/hmharness-adapter.test.ts `
  packages/host-runtime/test/app-server-host.test.ts `
  packages/renderer-extension/test/renderer-binding-probe.test.ts `
  packages/renderer-extension/test/versioned-renderer-adapter.test.ts `
  tests/release/production-renderer.test.mjs
```

Results:

- Dependencies installed: 277 packages.
- TypeScript build: passed.
- Typecheck: passed.
- Lint and package-boundary check: passed.
- Targeted tests: 6 files and 256 tests passed.
- Prettier check on all changed source/test/config files: passed.
- Direct `node --test tests/release/production-renderer.test.mjs` is not applicable because this Vitest test imports Vitest suites; the same file passed under the repository Vitest config.

HMHarness bridge:

- HMHarness commit: `a72596f637035073221d41cb542b081e45bae543`.
- Source bridge version: `0.5.3`.
- Bridge tests: 2 passed.
- Eight-package publish preflight and full HMHarness build/test/typecheck passed in the source monorepo.
- Current npm release: `0.5.2`, MIT, public.
- Installed `0.5.2` tarball returned its version, emitted the expected credential-free provider record, and did not leak a fake key.

Earlier live desktop smoke: HMHarness thread and turn reached `completed`; result availability was `available`; no bridge `run` process remained; renderer text contained `HMH-DONE`; and `正在思考` appeared zero times after completion.

## 中文

CodexHost PR 分支 `swsgbl:hmharness-adapter`，基准 `25fb54f`，提交 `3de8496`：

- 依赖安装：277 个包。
- TypeScript 构建：通过。
- typecheck：通过。
- lint 与 package-boundary：通过。
- 定向测试：6 个文件、256 个用例通过。
- 变更文件 Prettier 检查：通过。
- `node --test tests/release/production-renderer.test.mjs` 不是正确入口，因为该文件是 Vitest suite；使用仓库 Vitest 配置后通过。

HMHarness bridge：

- HMHarness 提交：`a72596f637035073221d41cb542b081e45bae543`。
- 源码 bridge 版本：`0.5.3`。
- bridge 测试：2 个通过。
- 八包发布预检和主仓库 typecheck/build/test 均通过。
- npm 当前版本：`0.5.2`，MIT，公开。
- 已真实安装 `0.5.2` tarball：版本输出通过，无凭据 provider 记录符合预期，假 key 未泄露。

此前真实桌面端冒烟：HMHarness thread 和 turn 均为 `completed`，result 可用，bridge `run` 进程退出，Renderer 中包含 `HMH-DONE`，完成后 `正在思考` 出现次数为 0。
