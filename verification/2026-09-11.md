# 2026-09-11 bridge release verification / bridge 发布验证

## English

- `npm view @hmharness/codexhost-bridge@0.5.3 --registry=https://registry.npmjs.org` returned MIT metadata, 4 files, 8,086 unpacked bytes, and `repository.directory` set to `packages/codexhost-bridge`.
- A clean project installed the official-registry tarball with `npm install <tarball> --ignore-scripts`.
- `hmh-codexhost --version` returned `{"version":"0.5.3"}`.
- `hmh-codexhost providers --json` against an isolated `HMH_HOME` returned the expected `alt-model` provider entry.
- The two fake API keys used by the isolated fixture were absent from both command outputs.

## 中文

- `npm view @hmharness/codexhost-bridge@0.5.3 --registry=https://registry.npmjs.org` 返回 MIT 元数据、4 个文件、8,086 bytes 未压缩大小，且 `repository.directory` 指向 `packages/codexhost-bridge`。
- 在干净项目中使用 `npm install <tarball> --ignore-scripts` 安装官方源 tarball。
- `hmh-codexhost --version` 返回 `{"version":"0.5.3"}`。
- 使用隔离的 `HMH_HOME` 执行 `hmh-codexhost providers --json`，返回预期的 `alt-model` provider 条目。
- 隔离配置中的两个 fake API key 均未出现在两条命令输出中。
