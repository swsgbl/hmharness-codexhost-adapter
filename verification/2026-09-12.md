# CodexHost 0.7.1 HMHarness validation

Date: 2026-09-12, Asia/Shanghai.

## Scope

- Update the official runtime to CodexHost `0.7.1` without pursuing another upstream merge.
- Adapt and preserve HMHarness in the independently maintained runtime.
- Verify build/tests, idempotent installation, launcher takeover, model selection, a real UI request, final UI cleanup, bridge cleanup, and protected local services.

## Current Versions

- `codexhost --version`: `0.7.1`; `npm view @codexhost/cli version`: `0.7.1`.
- Main and nested Windows platform packages: `@codexhost/cli@0.7.1` and `@codexhost/cli-win32-x64@0.7.1`.
- `codex --version`: `codex-cli 0.154.0`; `npm view @openai/codex version`: `0.154.0`.
- `codexhost inspect`: Desktop `26.908.4834.0`, package family `OpenAI.Codex_2p2nqsd0c76g0`, ASAR integrity reported.
- Runtime source base: official upstream commit `e6adb05095aff8aba5241230b07619c8b8aa8db4`.
- Bridge: globally linked `@hmharness/codexhost-bridge@0.6.5`; npm latest was also `0.6.5`.

## Build And Migration

- `npm run build:typescript` passed after the 0.7.1 three-way merge.
- Targeted test run: `270/270` passed.
- Bundled plugin SHA-256: `57D1EDD1394F2011B0FF4C88830870FF64C6D4A0A49BFA3BAF0C7094FDA4436A`.
- The bundle manifest uses the official `plugin.mjs` entry convention.
- `node --check scripts/apply-codexhost-0.7.1.mjs` passed.
- `node --check snapshots/codex-host-runtime/0.7.1/plugin/plugin.mjs` passed.
- `node scripts/apply-codexhost-0.7.1.mjs` was executed twice; both runs succeeded and the installed plugin hash remained `57D1...4436A`.

## Runtime Evidence

The desktop was launched through the verified chain:

```text
C:\Users\hongfu\Desktop\CodexHost.lnk
  -> C:\Windows\System32\wscript.exe
  -> C:\Users\hongfu\.codexhost-launch.vbs
  -> C:\Users\hongfu\.codexhost-launch.ps1
  -> @codexhost/cli@0.7.1 launch
```

- The active renderer CDP listener was loopback-only on `127.0.0.1:59909`.
- `codexhost inspect` reported the installed Desktop version and ASAR integrity hash.
- Protected listeners remained available after the HMHarness turn: CC Switch on `127.0.0.1:15721`, codex-image-proxy on `127.0.0.1:15731`, and the pre-existing DSH Web instance on `127.0.0.1:3081`.

## HMHarness Functional Smoke

- The renderer probe reported `hmharness` as enabled and `ready`.
- The agent selector showed `Select Agent, current HMHarness`.
- The model menu exposed all 14 configured HMHarness models.
- Active model: `glm / glm-5.3, glm-5.3`.
- A real composer turn requested and received `HMH_CODEXHOST_071_20260912_OK`.
- After completion, the probe still reported `state: ready`; visible `正在思考` / `Thinking` count was `0`.
- No command line containing the HM CodexHost bridge remained after the turn. Existing unrelated HM TUI/test-MCP processes were left untouched.

## DeepSeek Harness Machine-Local Repair

This work is not part of the HMHarness adapter backup:

- The global official DeepSeek plugin had been truncated (`1,077,002` bytes) and failed Node import. The truncated file was backed up and replaced with a source-built plugin (`1,158,027` bytes).
- The DSH Web profile referenced a nonexistent `codegraph` executable. That MCP entry was removed after backing up `cordis.patch.yml`; the pre-existing `3081` DSH Web service was not stopped.
- DSH still needed more than the official 20-second startup wait on this machine. The local DeepSeek adapter startup timeout was changed from 20 to 60 seconds, rebuilt, and installed; final plugin SHA-256 was `DC172706D8B59100BEA8ADF7888517EE46594AD21F76A49C7483D5BCAFEC1CB0`.
- After a complete CodexHost restart, the Connections page reported `DeepSeek Harness 正常`.

## Reproduction

```powershell
npm install -g @codexhost/cli@0.7.1
npm install -g @openai/codex@0.154.0
npm install -g @hmharness/codexhost-bridge@0.6.5
node scripts/apply-codexhost-0.7.1.mjs
node scripts/apply-codexhost-0.7.1.mjs  # idempotency check
codexhost --version
codexhost inspect
```

Then close CodexHost completely, launch it from `Desktop\CodexHost.lnk`, and verify the Connections page, agent/model selectors, one marker request, final Thinking count zero, and absence of an HM bridge child process.
